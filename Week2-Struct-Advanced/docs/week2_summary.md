# 2주차 총정리

## 이번 주 핵심 개념 5가지
1. 중첩 구조체로 복잡한 계층 데이터 모델링 가능 (BMS Pack 구조)
2. union: 같은 메모리를 여러 방식으로 "재해석"
3. bitfield: 변수를 비트 단위로 쪼개 사용, 넘치면 잘림
4. union+bitfield 조합: 레지스터 제어의 표준 패턴 
   (all로 전체 초기화, bit로 부분 접근)
5. 포인터 캐스팅 + volatile: 
   정해진 메모리 주소를 원하는 타입으로 다루는 하드웨어 제어 기법